// import './Header.scss';

import React from 'react';
import { Box, Typography } from '@mui/material';

const Header = () => (
  <Box component="header">
    <Typography variant="h1" className="header-text">Header</Typography>
  </Box>
);

export default Header;
